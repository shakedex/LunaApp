# Luna Web Docs Content (Milestone 9) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fill the scaffolded Astro Starlight site at `apps/docs` with the Milestone-9 content set — a splash landing page plus Overview, Privacy, Supported formats, Limitations, and FAQ — all hand-authored and code-verified against `packages/core`.

**Architecture:** Six content files under `apps/docs/src/content/docs/`, a copied logo asset under `apps/docs/src/assets/`, and one edit to `apps/docs/astro.config.mjs` (logo + explicit sidebar). Content pages are built first so that the landing's card links and the sidebar slugs all resolve before they are wired in the final task, keeping every build green. No new dependencies — Starlight built-in components only.

**Tech Stack:** Astro 7 + Starlight (`@astrojs/starlight` ^0.41.3), MDX/Markdown, Bun task runner. Verified via `astro check` (typecheck) and `astro build`; no automated UI/unit tests (project convention).

**Design spec:** `docs/superpowers/specs/2026-07-19-luna-web-docs-design.md`

## Global Constraints

- **Bun only.** Run docs scripts from the repo root: `bun --filter '@luna-web/docs' run <script>`. Available scripts: `dev`, `build`, `typecheck` (= `astro check`).
- **No new dependencies.** Use only Starlight built-ins (`splash` template, `hero` frontmatter, `<CardGrid>`/`<LinkCard>` from `@astrojs/starlight/components`, Markdown asides). Never hand-write a dependency version.
- **Touch only `apps/docs/`.** Do not modify anything under `apps/web`, `packages/core`, `tools/`, or `.github/`. Do not touch `apps/web/src/components/ui`.
- **Git discipline.** Stage by explicit file paths only — never `git add` a directory. **Never push** (the maintainer pushes personally). Run `git status` before staging; if a target file already has uncommitted changes you didn't make, STOP and coordinate.
- **No automated tests here.** Verification per task = `astro check` reports **0 errors** and `astro build` completes. The maintainer does visual QA in `dev`. Do not add Vitest/Playwright.
- **Base path is `/docs`.** Write internal links in content as absolute `/docs/<slug>/` (with leading and trailing slash).
- **Copy rules (non-negotiable).** RAW formats (`.braw`, `.r3d`, `.crm`, `.ari`) are **first-class clips** — never call them "undecodable," "unsupported," or a "raw notice." A file is a file: name, path, and size are always recorded. Do NOT reuse the stale wording from the 2026-07-17 design spec §3 ("No professional RAW").
- **Ground truth for format claims:** `packages/core/src/media/extensions.ts` (allowlist), `packages/core/src/thumbs/router.ts` (thumbnail routing), `packages/core/src/metadata/vendors/registry.ts` (vendor enrichers). Re-read before editing the Supported-formats or Limitations pages.

---

### Task 1: Overview and Privacy pages

**Files:**
- Create: `apps/docs/src/content/docs/what-it-is.md`
- Create: `apps/docs/src/content/docs/privacy.md`

**Interfaces:**
- Consumes: nothing (first task).
- Produces: pages at slugs `what-it-is` and `privacy`, linked by the landing card grid and sidebar in Task 5. Do not change `astro.config.mjs` yet — the default autogenerated sidebar keeps the build green.

- [ ] **Step 1: Write the Overview page**

Create `apps/docs/src/content/docs/what-it-is.md` with exactly:

```md
---
title: Overview
description: What Luna is, who it's for, and how it turns a camera card into a report.
---

Luna is a camera-report tool that runs entirely in your browser. Point it at a folder of
footage and it reads the card locally, groups clips into reels, extracts metadata and
thumbnails, and produces a clean, shareable report — with no upload, no install, and no
account.

## Who it's for

Luna is built for DITs (digital imaging technicians) and camera assistants who need to hand
off an accurate account of what's on a card: every file, its technical metadata, and a visual
reference frame where one can be produced.

## How a report comes together

1. **Pick a folder.** Luna asks for read access to a folder through your browser's File System
   Access API. Nothing is uploaded — the permission is local to your machine.
2. **Scan.** Luna walks the folder, lists every clip, and shows a pre-scan summary (clip count
   and total size) so you can confirm before any heavy work starts.
3. **Process.** For each clip, Luna extracts metadata and renders evenly-spaced thumbnails,
   running several clips in parallel. Progress and per-clip outcomes appear live.
4. **Reels.** Clips are grouped into reels by their recorded reel name, falling back to the
   top-level folder.
5. **Export.** Fill in the cover and branding fields, then export a **PDF** and/or **CSV**
   camera report. You save the file locally.

## What's in a report

A report lists each reel and the clips within it, with per-clip technical metadata (camera,
lens, ISO, shutter, color space, frame rate, duration, and more, wherever the file carries it),
thumbnails where Luna can produce them, and totals — clip counts and byte-accurate sizes for
every file on the card.

Read next: [Privacy](/docs/privacy/) · [Supported formats](/docs/supported-formats/)
```

- [ ] **Step 2: Write the Privacy page**

Create `apps/docs/src/content/docs/privacy.md` with exactly:

```md
---
title: Privacy
description: Why your footage never leaves your device.
---

**Your footage never leaves your device.** Luna reads your media locally through the File
System Access API and processes everything client-side. It does not upload footage, metadata,
or reports to any server.

## What Luna sends about your media: nothing

Luna has no analytics, no telemetry, and makes no network requests carrying your data. The
only network traffic is fetching the program itself:

- The **app bundle** (HTML, JavaScript, CSS), served like any website.
- The **WebAssembly engines** that decode media and read metadata, fetched from a public CDN
  (jsDelivr) the first time they're needed.

Neither carries anything about your footage — they are the same files for every visitor.

## First visit, then offline

On your first visit, Luna downloads its WebAssembly engines — the FFmpeg core (~31 MB) and the
MediaInfo module. The browser caches them, so later visits are instant and Luna keeps working
**offline**. Your media is read straight from disk on every run and held only in your browser's
memory while a report is being built.

:::note
Reports are generated in your browser and saved by you to a location you choose. Luna never
sees them.
:::

Read next: [Supported formats](/docs/supported-formats/) · [Limitations](/docs/limitations/)
```

- [ ] **Step 3: Typecheck**

Run: `bun --filter '@luna-web/docs' run typecheck`
Expected: completes with `0 errors` (warnings, if any, unrelated to these files).

- [ ] **Step 4: Build**

Run: `bun --filter '@luna-web/docs' run build`
Expected: build completes successfully; `apps/docs/dist/what-it-is/index.html` and `apps/docs/dist/privacy/index.html` are emitted.

- [ ] **Step 5: Commit**

```bash
git add apps/docs/src/content/docs/what-it-is.md apps/docs/src/content/docs/privacy.md
git commit -m "docs(web): overview and privacy pages"
```

---

### Task 2: Supported formats page

**Files:**
- Create: `apps/docs/src/content/docs/supported-formats.md`

**Interfaces:**
- Consumes: nothing at build time (docs do not import core). Content is verified against `packages/core/src/thumbs/router.ts`, `.../media/extensions.ts`, and `.../metadata/vendors/registry.ts`.
- Produces: page at slug `supported-formats`.

- [ ] **Step 1: Re-verify the ground truth**

Read these three files and confirm the table below still matches before writing:
- `packages/core/src/thumbs/router.ts` — `MEDIABUNNY_EXTENSIONS`, `FFMPEG_EXTENSIONS`, `PREVIEW_ONLY_EXTENSIONS` (`.crm`, `.r3d`), `NO_THUMBNAIL_EXTENSIONS` (`.braw`, `.ari`), and the ProRes-RAW → `preview` override.
- `packages/core/src/media/extensions.ts` — the 16-entry allowlist.
- `packages/core/src/metadata/vendors/registry.ts` — enrichers: ARRI (mov + mxf), acquisition (Sony/Canon), BRAW, Panasonic.

If any bucket has changed, update the table in Step 2 to match the code (the code wins).

- [ ] **Step 2: Write the Supported formats page**

Create `apps/docs/src/content/docs/supported-formats.md` with exactly:

```md
---
title: Supported formats
description: Which containers and RAW formats Luna handles, and how each gets a thumbnail.
---

Luna surfaces **every** file on a card. A file is a file: its name, full path, and size are
always recorded and counted in the report totals, whether or not Luna can paint a thumbnail for
it. Most clips also get metadata and evenly-spaced thumbnails.

## How each format is handled

Luna decides how to produce a thumbnail from the file's extension (and, for ProRes RAW, its
codec):

| Handling | Formats |
| --- | --- |
| **Hardware-decoded thumbnails** (WebCodecs) | `.mov` · `.mp4` · `.m4v` · `.mkv` · `.webm` · `.3gp` |
| **Software-decoded thumbnails** (FFmpeg WASM — slower) | `.mxf` · `.avi` · `.mts` · `.m2ts` · `.wmv` · `.flv` |
| **Embedded / sidecar preview frame** | `.crm` · `.r3d` · ProRes RAW `.mov` |
| **Metadata + placeholder thumbnail** | `.braw` · `.ari` |

ProRes clips decode through a dedicated high-performance path. **ProRes RAW** is detected by its
codec and uses its embedded preview frame rather than a full decode.

## RAW formats are first-class clips

Blackmagic RAW (`.braw`), REDCODE (`.r3d`), Canon Cinema RAW Light (`.crm`), and ARRIRAW
(`.ari`) are treated as full clips, not second-class citizens:

- **Metadata** is extracted wherever the format carries it.
- **`.crm` and `.r3d`** show a real reference frame from the file's embedded preview or its
  `.rtn` sidecar when present.
- **`.braw` and `.ari`** show an honest placeholder thumbnail — the browser can't paint a frame
  from them without the vendor SDK — while still appearing in their reel with full name, path,
  size, and any available metadata.

These files are never labelled "undecodable" or dropped from the report.

## Metadata enrichment

On top of the container's generic metadata, Luna applies vendor-aware enrichment for common
camera families — ARRI (QuickTime and MXF), Sony and Canon acquisition metadata, Blackmagic
RAW, and Panasonic. Camera fields (ISO, white balance, lens, and so on) appear only when the
file actually carries them.

:::note[Source of truth]
Format handling is defined in the app's core: the supported-extension allowlist in
`packages/core/src/media/extensions.ts`, the thumbnail routing in
`packages/core/src/thumbs/router.ts`, and the vendor enrichers in
`packages/core/src/metadata/vendors/registry.ts`.
:::

Read next: [Limitations](/docs/limitations/)
```

- [ ] **Step 3: Typecheck**

Run: `bun --filter '@luna-web/docs' run typecheck`
Expected: `0 errors`.

- [ ] **Step 4: Build**

Run: `bun --filter '@luna-web/docs' run build`
Expected: build completes; `apps/docs/dist/supported-formats/index.html` is emitted.

- [ ] **Step 5: Commit**

```bash
git add apps/docs/src/content/docs/supported-formats.md
git commit -m "docs(web): supported formats page"
```

---

### Task 3: Limitations page

**Files:**
- Create: `apps/docs/src/content/docs/limitations.md`

**Interfaces:**
- Consumes: the format handling described in Task 2 (kept consistent — placeholder vs. preview split must match `router.ts`).
- Produces: page at slug `limitations`.

- [ ] **Step 1: Write the Limitations page**

Create `apps/docs/src/content/docs/limitations.md` with exactly:

```md
---
title: Limitations
description: Honest constraints — browser support, decode speed, RAW thumbnails, metadata.
---

Luna is built around an honest report. These are its real constraints.

## Chromium-only

Luna relies on the File System Access API and WebCodecs, which today are available in
Chromium-based browsers — Chrome, Edge, Brave, and Arc. Firefox and Safari don't yet expose both
APIs, so Luna doesn't run there.

## Software decode is slower

Clips that go through the FFmpeg WASM path (`.mxf`, DNxHD, and other legacy containers) are
decoded purely in software. Large MXF cards on a slower machine will feel it. Clips on the
hardware path (H.264, HEVC, VP9, AV1, and ProRes) are much faster.

## Some RAW clips show placeholder thumbnails

`.braw` and `.ari` show a placeholder thumbnail — the browser can't decode a frame from them
without the vendor SDK. `.crm` and `.r3d` show an embedded or sidecar preview frame when one is
present. In every case the clip is still fully catalogued: name, path, size, and any metadata
the file carries.

## Metadata depends on the format

Camera fields — ISO, white balance, lens, color space, and the rest — appear only when the
container actually records them. A format that doesn't store a field simply leaves it blank;
Luna never invents values.

## First visit downloads the engines

The first time you use Luna, it downloads its WebAssembly engines (~31 MB). After that they're
cached and Luna runs offline.

:::caution
A "limitation" here never means a file is hidden or dropped. Every file on the card is surfaced
and counted, even when Luna can't paint a thumbnail for it.
:::

Read next: [FAQ](/docs/faq/)
```

- [ ] **Step 2: Typecheck**

Run: `bun --filter '@luna-web/docs' run typecheck`
Expected: `0 errors`.

- [ ] **Step 3: Build**

Run: `bun --filter '@luna-web/docs' run build`
Expected: build completes; `apps/docs/dist/limitations/index.html` is emitted.

- [ ] **Step 4: Commit**

```bash
git add apps/docs/src/content/docs/limitations.md
git commit -m "docs(web): limitations page"
```

---

### Task 4: FAQ page

**Files:**
- Create: `apps/docs/src/content/docs/faq.md`

**Interfaces:**
- Consumes: cross-links to `what-it-is`, `privacy`, `supported-formats`, `limitations` (all created in Tasks 1–3).
- Produces: page at slug `faq`.

- [ ] **Step 1: Write the FAQ page**

Create `apps/docs/src/content/docs/faq.md` with exactly:

```md
---
title: FAQ
description: Quick answers to the most common questions about Luna.
---

## Which browsers work?

Chromium-based browsers — Chrome, Edge, Brave, and Arc. Luna needs the File System Access API
and WebCodecs, which Firefox and Safari don't fully support yet. See
[Limitations](/docs/limitations/).

## Does my footage get uploaded anywhere?

No. Everything runs in your browser and your media never leaves your device. The only network
requests fetch the program itself. See [Privacy](/docs/privacy/).

## Why do some RAW clips have no thumbnail?

`.braw` and `.ari` can't be decoded to a frame in the browser without the camera vendor's SDK,
so they show a placeholder. `.crm` and `.r3d` show an embedded or sidecar preview when the file
has one. Either way the clip is fully listed with its metadata, path, and size. See
[Supported formats](/docs/supported-formats/).

## Why is a large MXF card slow?

MXF and other legacy containers decode in software (FFmpeg WASM) rather than on your machine's
hardware video decoders. It works, but it's slower than the hardware path used for H.264, HEVC,
and ProRes.

## Does Luna work offline?

Yes, after the first visit. Luna downloads its WebAssembly engines once (~31 MB), the browser
caches them, and later runs work without a connection.

## Is Luna free and open-source?

Yes. Luna is licensed under the Apache License 2.0. The source is on
[GitHub](https://github.com/shakedex/LunaApp).

## Is Luna affiliated with ARRI, Blackmagic, Sony, or RED?

No. Luna is an independent open-source project and is not affiliated with, endorsed by, or
sponsored by any camera manufacturer. All product names and trademarks belong to their
respective owners. See the full
[Disclaimer](https://github.com/shakedex/LunaApp/blob/master/DISCLAIMER.md).
```

- [ ] **Step 2: Typecheck**

Run: `bun --filter '@luna-web/docs' run typecheck`
Expected: `0 errors`.

- [ ] **Step 3: Build**

Run: `bun --filter '@luna-web/docs' run build`
Expected: build completes; `apps/docs/dist/faq/index.html` is emitted.

- [ ] **Step 4: Commit**

```bash
git add apps/docs/src/content/docs/faq.md
git commit -m "docs(web): faq page"
```

---

### Task 5: Landing splash, logo asset, and sidebar config

**Files:**
- Create: `apps/docs/src/assets/luna-logo-lg.webp` (copied from `docs/branding/luna-logo-lg.webp`)
- Modify: `apps/docs/src/content/docs/index.mdx` (rewrite the placeholder as a splash landing)
- Modify: `apps/docs/astro.config.mjs` (add `logo` and explicit `sidebar`)

**Interfaces:**
- Consumes: all five content pages from Tasks 1–4 (their slugs are referenced by the card grid and the sidebar) and the copied logo asset.
- Produces: the final landing page and navigation. This is the capstone — after it, the full site builds with the logo, ordered sidebar, and resolving card links.

- [ ] **Step 1: Copy the logo asset**

Run:
```bash
mkdir -p apps/docs/src/assets && cp docs/branding/luna-logo-lg.webp apps/docs/src/assets/luna-logo-lg.webp
```
Expected: `apps/docs/src/assets/luna-logo-lg.webp` exists (verify with `ls -l apps/docs/src/assets/luna-logo-lg.webp`).

- [ ] **Step 2: Rewrite the landing page as a splash**

Overwrite `apps/docs/src/content/docs/index.mdx` with exactly:

```mdx
---
title: Luna Web
description: Camera reports in your browser. Nothing leaves your device.
template: splash
hero:
  tagline: Camera reports in your browser. Nothing leaves your device.
  image:
    alt: Luna
    file: ../../assets/luna-logo-lg.webp
  actions:
    - text: Open Luna
      link: https://luna.ozer2.one/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/shakedex/LunaApp
      icon: external
      variant: minimal
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components'

Luna turns a camera card into a shareable PDF/CSV camera report — entirely in your browser.
Point it at a folder of clips and it detects reels, extracts metadata and thumbnails, and builds
a clean report. Your footage never leaves your device.

<CardGrid>
	<LinkCard title="Overview" href="/docs/what-it-is/" description="What Luna is, who it's for, and how a report comes together." />
	<LinkCard title="Privacy" href="/docs/privacy/" description="Why your footage never leaves your device." />
	<LinkCard title="Supported formats" href="/docs/supported-formats/" description="Which containers and RAW formats Luna handles, and how." />
	<LinkCard title="Limitations" href="/docs/limitations/" description="Honest constraints — browser support, speed, RAW thumbnails." />
	<LinkCard title="FAQ" href="/docs/faq/" description="Quick answers to the most common questions." />
</CardGrid>
```

- [ ] **Step 3: Add logo and sidebar to the Astro config**

Overwrite `apps/docs/astro.config.mjs` with exactly:

```js
import starlight from '@astrojs/starlight'
import { defineConfig } from 'astro/config'

export default defineConfig({
  site: 'https://luna.ozer2.one',
  base: '/docs',
  integrations: [
    starlight({
      title: 'Luna Web',
      description: 'Camera reports in your browser. Nothing leaves your device.',
      logo: {
        src: './src/assets/luna-logo-lg.webp',
      },
      social: [
        {
          icon: 'github',
          label: 'GitHub',
          href: 'https://github.com/shakedex/LunaApp',
        },
      ],
      sidebar: [
        { label: 'Overview', slug: 'what-it-is' },
        { label: 'Privacy', slug: 'privacy' },
        { label: 'Supported formats', slug: 'supported-formats' },
        { label: 'Limitations', slug: 'limitations' },
        { label: 'FAQ', slug: 'faq' },
      ],
    }),
  ],
})
```

- [ ] **Step 4: Typecheck**

Run: `bun --filter '@luna-web/docs' run typecheck`
Expected: `0 errors`.

- [ ] **Step 5: Build**

Run: `bun --filter '@luna-web/docs' run build`
Expected: build completes with no broken-link or missing-image errors; `apps/docs/dist/index.html` contains the hero and the five link cards.

- [ ] **Step 6: Commit**

```bash
git add apps/docs/src/assets/luna-logo-lg.webp apps/docs/src/content/docs/index.mdx apps/docs/astro.config.mjs
git commit -m "docs(web): splash landing, logo, and sidebar"
```

- [ ] **Step 7: Maintainer visual QA (handoff)**

Hand the maintainer this checklist to run `bun --filter '@luna-web/docs' run dev` and confirm in the browser at `/docs`:
- Splash landing shows the Luna logo, tagline, and **Open Luna** / **GitHub** buttons.
- The five cards link to the right pages.
- Sidebar order reads Overview → Privacy → Supported formats → Limitations → FAQ; the landing is not in the sidebar.
- Each page renders; asides (note/caution) display; internal "Read next" links work.
- Logo appears in the header next to the "Luna Web" title.

---

## Self-Review

**1. Spec coverage** (against `docs/superpowers/specs/2026-07-19-luna-web-docs-design.md`):
- §6 site config (logo + sidebar) → Task 5, Steps 1 & 3. ✓
- §7.1 splash landing → Task 5, Step 2. ✓
- §7.2 Overview → Task 1. ✓  §7.3 Privacy → Task 1. ✓
- §7.4 Supported formats → Task 2. ✓  §7.5 Limitations → Task 3. ✓  §7.6 FAQ → Task 4. ✓
- §8 Starlight built-ins only → Global Constraints + used in Tasks 2/3/5. ✓
- §9 verification (`astro check` + build + visual QA) → every task's steps + Task 5 Step 7. ✓
- §10 non-goals (no deploy, no favicon-required, no extra pages) → not scheduled. ✓
- §11 files touched → matches the five tasks exactly. ✓

**2. Placeholder scan:** No "TBD"/"TODO"/"handle edge cases" — every page's full content is inline. ✓

**3. Type/name consistency:** Slugs are consistent everywhere — `what-it-is`, `privacy`, `supported-formats`, `limitations`, `faq`. The sidebar `slug` values (Task 5 Step 3), the landing `href="/docs/<slug>/"` values (Task 5 Step 2), and the created filenames (Tasks 1–4) all match. Logo path is identical in the copy step (`apps/docs/src/assets/luna-logo-lg.webp`), the config `logo.src` (`./src/assets/luna-logo-lg.webp`), and the hero `image.file` (`../../assets/luna-logo-lg.webp`, relative to `index.mdx`). ✓

**4. Ordering safety:** Content pages (Tasks 1–4) land before the landing card grid and sidebar reference them (Task 5), and the logo asset is copied (Task 5 Step 1) before it is referenced (Steps 2–3) and before the build (Step 5) — so every task's build is green. ✓
